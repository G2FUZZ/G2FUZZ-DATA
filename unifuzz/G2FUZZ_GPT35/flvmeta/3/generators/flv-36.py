# Save generated files into ./tmp/
import os

# Create a directory if it doesn't exist
if not os.path.exists('./tmp/'):
    os.makedirs('./tmp/')

# Generate files and save them into ./tmp/
# Your code for generating and saving files goes here