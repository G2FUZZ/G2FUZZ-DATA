# You should save the generated files into `./tmp/`

import os

# Create a directory named 'tmp' if it doesn't exist
if not os.path.exists('./tmp'):
    os.makedirs('./tmp')

# Save the generated files into the 'tmp' directory
# Your code for generating and saving files goes here